/**
 * @file snmp.h
 * @brief Подсистема обработки SNMP-протокола.
 */

/**
 * @defgroup snmp SNMP subsystem
 * @brief Подсистема обработки SNMP-запросов и ответов.
 *
 * Модуль реализует приём и обработку SNMP-запросов
 * (v1 / v2c), включая:
 * - приём UDP-пакетов;
 * - декодирование ASN.1 BER;
 * - формирование SNMP-ответов;
 * - взаимодействие с хранилищем данных UPS.
 *
 * Подсистема отделена от доменной модели UPS и
 * использует её через абстрактный интерфейс доступа к данным.
 *
 * Файл предназначен исключительно для документирования
 * архитектуры подсистемы и не должен подключаться в коде.
 */
