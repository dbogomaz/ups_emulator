/**
 * @file ups.h
 * @brief Доменная модель источника бесперебойного питания (UPS).
 */

/**
 * @defgroup ups UPS domain model
 * @brief Доменная модель эмулятора источника бесперебойного питания.
 *
 * Модуль содержит типы, константы, параметры и вспомогательные структуры,
 * описывающие состояние и конфигурацию ИБП.
 *
 * Включает в себя:
 * - типы и перечисления, используемые в доменной логике ИБП;
 * - описание OID и соответствующих им параметров;
 * - структуры и классы для хранения и инициализации данных модели ИБП.
 *
 * Модуль используется несколькими подсистемами проекта (SNMP-агент, эмулятор, GUI).
 *
 * Файл предназначен исключительно для документирования архитектуры
 * и не должен подключаться в коде.
 */
