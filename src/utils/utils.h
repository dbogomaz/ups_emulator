/**
 * @file utils.h
 * @brief Архитектурный модуль вспомогательных утилит.
 *
 * Файл служит якорем документации для модуля utils и не предназначен
 * для подключения в коде. Содержит только описание архитектуры,
 * групп и пространств имён.
 */

/**
 * @defgroup utils Utilities
 * @brief Вспомогательные утилиты общего назначения.
 *
 * Модуль содержит независимые вспомогательные функции и классы,
 * используемые в различных частях проекта (CLI, GUI, SNMP и др.).
 *
 * Утилиты не зависят от доменной логики ИБП и не содержат состояния,
 * связанного с жизненным циклом эмулятора.
 */

/**
 * @namespace utils
 * @brief Пространство имён для вспомогательных утилит.
 *
 * Содержит функции и классы общего назначения, такие как:
 * - работа с файловой системой;
 * - обработка строк;
 * - разбор конфигурационных файлов.
 *
 * Пространство имён не привязано к конкретной подсистеме проекта
 * и может использоваться повторно.
 */
namespace utils {
    // Намеренно пусто.
    // Пространство имён объявлено исключительно
    // для целей документирования.
}
